function sayHello(name) {
  debugger; //programnya berhenti disini untuk dievaluasi oleh si coder
  return `Hello ${name}, Welcome`;
}

const name = "Rendi Sagala";
debugger; //debugging berhenti disini
console.log(sayHello(name));

//node inspect namafile.js (UNTUK DEBUGGING)
